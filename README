PROPOSED STEPS:
	1. Convert data.frame to list for data.frame using = 
list(date=É,data=É,type=timeSeries)
	2. Add a tz export function.
	3. Get all functions to handle multiple datasets
	4. investigate using (cummulative.month-cummulative.month%%multiple)%%12 for calculating the months
TODO:
	* test on windows/linux
PROBLEMS:
	* Wont handle more than 12 month blocks.
WISHLIST:
	* Convert the timeSeries objects to a class.

COMPLETED:
	*Working On a mac.

